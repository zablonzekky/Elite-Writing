import React from 'react';

const PricingPage = () => (
  <div className="container py-4">
    <h1 className="h4 fw-bold">Pricing</h1>
    <p className="text-muted">Simple transparent plans for every career stage.</p>
  </div>
);

export default PricingPage;
